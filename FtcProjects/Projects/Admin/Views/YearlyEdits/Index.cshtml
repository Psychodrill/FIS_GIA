<br />
<ul id="SelectAnAction">
    <li><a href="#" id="CompositionThemes">Добавление тем сочинений</a></li>
    @*<li><a href="#" ">Актуализация сведений о минимальных баллах ЕГЭ</a></li>
        <li><a href="#" ">Актуализация сведений о действующих результатах ЕГЭ</a></li>
        <li><a href="#" ">Актуализация сведений о минимальных баллах ЕГЭ для подтверждения диплома по олимпиаде</a></li>*@
</ul>

<br />
@*@await Html.PartialAsync("~/Views/CompositionThemes/Index.cshtml")*@

<div class="form-group">
    <div class="col-md-8">
        <div id="result" />
    </div>
</div>



<script type="text/javascript">

    $(document).ready(function () {
        $('#CompositionThemes').on('click', function (e) {
                console.log('clicked');
                 $.ajax({
                    type: "GET",
                    url: '@Url.Action("Index", "CompositionThemes")',
                    success: function (msg) {
                        $('#result').empty().html(msg);
                        console.log('success');
                    }
                });
            });

            @*$(document).on('click', function (e) {
                var clicked = e.target.id;
                console.log(clicked);
                 $.ajax({
                    type: "POST",
                    url: '@Url.Action("Index")',
                    success: function (msg) {
                        $('#result').empty().html(msg);
                        console.log('success');
                    }
                });
            });*@
    });

</script>