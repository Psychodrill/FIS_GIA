<br />
<ul>
    <li><a href="#" id="EditInstitution">Изменение и загрузка сведений о лицензии и аккредитации ОО</a></li>
    <li><a href="#" id="DelBadDocs">Удаление документов, ошибочно загруженных ОО</a></li>
</ul>
<br />

<div class="form-group">
    <div class="col-md-8">
        <div id="result" />
    </div>
</div>

    <script type="text/javascript">
   $(document).ready(function () {
       $('#EditInstitution').click(function (e) {
            e.preventDefault();
            $.ajax({
                type: "POST",
                url: '@Url.Action("EditInstitutionData", "Institution")',
                success: function (msg) {
                $('#result').empty().html(msg);
                }
            });
        });


        $('#DelBadDocs').click(function (e) {
            e.preventDefault();
            $.ajax({
                type: "POST",
                url: '@Url.Action("DelBadDocs", "Institution")',
                success: function (msg) {
                $('#result').empty().html(msg);
                }
            });
        });

   });
    </script>









